<Window x:Class="AutomaticUpdater.Windows.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AutomaticUpdater — Update Log"
        Width="800" Height="500"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Log TextBox -->
        <TextBox x:Name="TxtLog"
                 Grid.Row="0"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="#1E1E1E"
                 Foreground="#D4D4D4"
                 BorderThickness="0"
                 Padding="8"
                 CaretBrush="#D4D4D4"/>

        <!-- Status Bar -->
        <Border Grid.Row="1"
                Background="#2D2D2D"
                BorderBrush="#444444"
                BorderThickness="0,1,0,0"
                Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TxtStatus"
                           Grid.Column="0"
                           Text="Ready"
                           Foreground="#AAAAAA"
                           VerticalAlignment="Center"
                           FontSize="11"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Refresh" Width="70" Margin="0,0,6,0"
                            Click="OnRefresh"/>
                    <Button Content="Clear Log" Width="75"
                            Click="OnClearLog"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
